{
  "buildCommand": "npm --prefix code run build",
  "outputDirectory": "code/dist",
  "installCommand": "npm install && npm --prefix code install",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
